from interpreter import interpreter

task_description = ("Write a python script that show me the forecast for tomorrow "
                    "(min and max temperature). My place should be detected by IP."
                    "Use free API or web scraping.")

interpreter.start_terminal_interface()

_ = input("Press any key to exit...")
